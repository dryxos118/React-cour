Pour installer le linter de create-react-app
Installer l'extension esLint dans VsCode

```js
npm i -D eslint-config-react-app
```

Créer à la racine du projet un fichier .eslintrc.json

```json
{
  "extends": "react-app"
}
```
